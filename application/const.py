from application.models.attrdict import attrdict_const as dict

response = dict(
    body = dict(
        
    ),
    action = dict(
        abort = 'abort',
        alert = 'alert',
        redirect = 'redirect',
        none = None
    )
)